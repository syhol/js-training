
// Run: npm run test2

function splitString(subject) {
  // Hint: try the string split method
}

test('We can split a string', () => {
  expect(splitString('hello')).toEqual(['h', 'e', 'l', 'l', 'o']);
  expect(splitString('why')).toEqual(['w', 'h', 'y']);
});
